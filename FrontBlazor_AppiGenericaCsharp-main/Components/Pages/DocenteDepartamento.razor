@page "/docentedepartamento"
@rendermode InteractiveServer
@inject FrontBlazor_AppiGenericaCsharp.Services.ApiService Api

<h3>CRUD Docente Departamento</h3>

<div class="mb-3"><label>Docente</label><input class="form-control" @bind="docente" /></div>

<div class="mb-3">
    <label>Departamento</label>
    <select class="form-control" @bind="departamento">
        <option value="">Seleccione una facultad</option>
        @foreach (var fac in facultadesLista)
        {
            <option value="@fac["id"]">@fac["nombre"]</option>
        }
    </select>
</div>

<div class="mb-3"><label>Dedicación</label><input class="form-control" @bind="dedicacion" /></div>
<div class="mb-3"><label>Modalidad</label><input class="form-control" @bind="modalidad" /></div>
<div class="mb-3"><label>Fecha ingreso</label><input type="date" class="form-control" @bind="fechaIngreso" /></div>
<div class="mb-3"><label>Fecha salida</label><input type="date" class="form-control" @bind="fechaSalida" /></div>

<div class="mb-3">
    <button class="btn btn-primary me-2" @onclick="Guardar">Guardar</button>
    <button class="btn btn-secondary" @onclick="Limpiar">Limpiar</button>
</div>

@if (!string.IsNullOrWhiteSpace(mensaje))
{
    <div class="alert alert-info">@mensaje</div>
}

<h4>Listado Docente Departamento</h4>
<table class="table table-bordered">
    <thead>
        <tr><th>Docente</th><th>Departamento</th><th>Dedicación</th><th>Modalidad</th><th>Fecha ingreso</th><th>Fecha salida</th><th>Acciones</th></tr>
    </thead>
    <tbody>
        @foreach (var item in registros)
        {
            var docenteEliminar = item["docente"]?.ToString() ?? "";
            var depEliminar = item["departamento"]?.ToString() ?? "";

            <tr>
                <td>@item["docente"]</td>
                <td>@item["departamento"]</td>
                <td>@item["dedicacion"]</td>
                <td>@item["modalidad"]</td>
                <td>@item["fechaIngreso"]</td>
                <td>@item["fechaSalida"]</td>
                <td>
                    <button class="btn btn-sm btn-danger" @onclick="() => Eliminar(docenteEliminar, depEliminar)">Eliminar</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@code {
    private List<Dictionary<string, object?>> registros = new();
    private List<Dictionary<string, object?>> facultadesLista = new();

    private string docente = "", departamento = "", dedicacion = "", modalidad = "", mensaje = "";
    private DateTime? fechaIngreso;
    private DateTime? fechaSalida;

    protected override async Task OnInitializedAsync()
    {
        await CargarDatos();
        await CargarFacultades();
    }

    private async Task CargarDatos() => registros = await Api.ListarAsync("DocenteDepartamento");
    private async Task CargarFacultades() => facultadesLista = await Api.ListarAsync("Facultad");

    private async Task Guardar()
    {
        var datos = new Dictionary<string, object?>
        {
            { "docente", docente },
            { "departamento", departamento },
            { "dedicacion", dedicacion },
            { "modalidad", modalidad },
            { "fechaIngreso", fechaIngreso?.ToString("yyyy-MM-dd") },
            { "fechaSalida", fechaSalida?.ToString("yyyy-MM-dd") }
        };

        var resultado = await Api.CrearAsync("DocenteDepartamento", datos);
        mensaje = resultado.mensaje;

        if (resultado.exito)
        {
            Limpiar();
            await CargarDatos();
        }
    }

    private async Task Eliminar(string docenteEliminar, string depEliminar)
    {
        var resultado = await Api.EliminarCompuestoAsync("DocenteDepartamento",
            new Dictionary<string, string>
            {
                { "docente", docenteEliminar },
                { "departamento", depEliminar }
            });

        mensaje = resultado.mensaje;
        await CargarDatos();
    }

    private void Limpiar()
    {
        docente = "";
        departamento = "";
        dedicacion = "";
        modalidad = "";
        fechaIngreso = null;
        fechaSalida = null;
        mensaje = "";
    }
}