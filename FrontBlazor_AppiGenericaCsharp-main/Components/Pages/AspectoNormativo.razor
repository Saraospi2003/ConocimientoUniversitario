@page "/aspectonormativo"
@rendermode InteractiveServer
@inject FrontBlazor_AppiGenericaCsharp.Services.ApiService Api

<h3>CRUD Aspecto Normativo</h3>

<div class="mb-3">
    <label>ID</label>
    <input class="form-control" @bind="id" />
</div>
<div class="mb-3">
    <label>Tipo</label>
    <input class="form-control" @bind="tipo" />
</div>
<div class="mb-3">
    <label>Descripción</label>
    <input class="form-control" @bind="descripcion" />
</div>
<div class="mb-3">
    <label>Fuente</label>
    <input class="form-control" @bind="fuente" />
</div>

<div class="mb-3">
    <button class="btn btn-primary me-2" @onclick="Guardar">Guardar</button>
    <button class="btn btn-warning me-2" @onclick="Actualizar">Actualizar</button>
    <button class="btn btn-secondary" @onclick="Limpiar">Limpiar</button>
</div>

@if (!string.IsNullOrWhiteSpace(mensaje))
{
    <div class="alert alert-info">@mensaje</div>
}

<h4>Listado de aspectos normativos</h4>
<table class="table table-bordered">
    <thead>
        <tr><th>ID</th><th>Tipo</th><th>Descripción</th><th>Fuente</th><th>Acciones</th></tr>
    </thead>
    <tbody>
        @foreach (var item in aspectos)
        {
            var idEliminar = item["id"]?.ToString() ?? "";
            <tr>
                <td>@item["id"]</td>
                <td>@item["tipo"]</td>
                <td>@item["descripcion"]</td>
                <td>@item["fuente"]</td>
                <td>
                    <button class="btn btn-sm btn-info me-1" @onclick="() => Seleccionar(item)">Editar</button>
                    <button class="btn btn-sm btn-danger" @onclick="() => Eliminar(idEliminar)">Eliminar</button>
                </td>
            </tr>
        }
    </tbody>
</table>

@code {
    private List<Dictionary<string, object?>> aspectos = new();
    private string id = "", tipo = "", descripcion = "", fuente = "", mensaje = "";

    protected override async Task OnInitializedAsync() => await CargarDatos();
    private async Task CargarDatos() => aspectos = await Api.ListarAsync("aspecto_normativo");

    private async Task Guardar()
    {
        if (string.IsNullOrWhiteSpace(id) || string.IsNullOrWhiteSpace(tipo) ||
            string.IsNullOrWhiteSpace(descripcion) || string.IsNullOrWhiteSpace(fuente))
        {
            mensaje = "Completa todos los campos."; StateHasChanged(); return;
        }
        var datos = new Dictionary<string, object?> { { "id", id }, { "tipo", tipo }, { "descripcion", descripcion }, { "fuente", fuente } };
        var resultado = await Api.CrearAsync("aspecto_normativo", datos);
        mensaje = resultado.mensaje;
        if (resultado.exito) { Limpiar(); await CargarDatos(); }
    }

    private async Task Actualizar()
    {
        if (string.IsNullOrWhiteSpace(id) || string.IsNullOrWhiteSpace(tipo) ||
            string.IsNullOrWhiteSpace(descripcion) || string.IsNullOrWhiteSpace(fuente))
        {
            mensaje = "Completa todos los campos."; StateHasChanged(); return;
        }
        var datos = new Dictionary<string, object?> { { "id", id }, { "tipo", tipo }, { "descripcion", descripcion }, { "fuente", fuente } };
        var resultado = await Api.ActualizarAsync("aspecto_normativo", "id", id, datos);
        mensaje = resultado.mensaje;
        if (resultado.exito) { Limpiar(); await CargarDatos(); }
    }

    private async Task Eliminar(string idEliminar)
    {
        var resultado = await Api.EliminarAsync("aspecto_normativo", "id", idEliminar);
        mensaje = resultado.mensaje;
        await CargarDatos();
    }

    private void Seleccionar(Dictionary<string, object?> item)
    {
        id = item["id"]?.ToString() ?? "";
        tipo = item["tipo"]?.ToString() ?? "";
        descripcion = item["descripcion"]?.ToString() ?? "";
        fuente = item["fuente"]?.ToString() ?? "";
        mensaje = "Registro cargado para edición.";
    }

    private void Limpiar() { id = ""; tipo = ""; descripcion = ""; fuente = ""; mensaje = ""; }
}
